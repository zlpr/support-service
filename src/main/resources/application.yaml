spring:
datasource:
  url: jdbc:h2:mem:default
  username: sa
  password:
  driver-class-name: org.h2.Driver
  jpa:
    database: h2
    hibernate:
      ddl-auto: none
    show-sql: true
h2:
  console:
    enabled: true
management:
  endpoints:
    web:
      exposure:
        include: "*"